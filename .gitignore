# Temporary directories
tmp/
temp/
.tmp/

# Test artifacts
test-git-repo/
test-output/
*.log
*.tmp

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE files
.vscode/
.idea/
*.swp
*.swo
*.claude
*~
.vim/
.netrwhist

# Bash history and cache
.bash_history
.bashrc_local

# Local development files
local-*
dev-*
scratch/
playground/

# Build artifacts (if we add them later)
dist/
build/
release/

# Downloaded dependencies (like bats-core we downloaded for testing)
bats-core*/
node_modules/

# Environment-specific config
.env
.env.local
.env.*.local

# Coverage reports (if we add code coverage)
coverage/
*.coverage
htmlcov/

# Package manager files (if we add them)
package-lock.json
yarn.lock

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Local configuration that shouldn't be committed
config.local
*.local

# Backup files
*.bak
*.backup
*~

# Test configuration that might contain secrets
test/.env
test/config.local

# gh-context repo bindings (optional - you may want to commit these)
.ghcontext


